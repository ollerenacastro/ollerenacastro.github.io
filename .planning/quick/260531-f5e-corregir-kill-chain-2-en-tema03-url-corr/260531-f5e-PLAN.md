---
phase: quick
plan: 260531-f5e
type: execute
wave: 1
depends_on: []
files_modified:
  - _posts/2026-05-31-tema03.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "La URL de Jenkins Script Console en el paso 1 del lab es http://10.0.2.15:8484/script (sin /jenkins/)"
    - "El lab incluye un paso de verificación de servicio antes de abrir el browser"
    - "El paso de verificación enseña al menos dos formas de confirmar que Jenkins responde"
  artifacts:
    - path: "_posts/2026-05-31-tema03.md"
      provides: "Kill Chain 2 corregida con URL correcta y paso de verificación"
      contains: "http://10.0.2.15:8484/script"
  key_links:
    - from: "paso de verificación"
      to: "paso 1 (abrir browser)"
      via: "orden numérica en el lab"
      pattern: "8484/script"
---

<objective>
Corregir dos errores en la sección "## Kill Chain 2: Jenkins Script Console RCE" del post
_posts/2026-05-31-tema03.md:

1. La URL `http://10.0.2.15:8484/jenkins/script` produce HTTP 404 porque Jenkins standalone
   (Winstone/Jetty) usa context path `/` — la URL correcta es `http://10.0.2.15:8484/script`.

2. El lab no tiene ningún paso que verifique que Jenkins está levantado antes de intentar
   conectar. Agregar un paso de verificación con comandos PowerShell/CMD (desde Metasploitable3)
   y con curl (desde Kali).

Purpose: Evitar que el alumno pierda tiempo depurando un 404 que parece un problema de
         configuración suyo, y enseñar el hábito de verificar el estado del servicio antes
         de atacarlo.
Output: _posts/2026-05-31-tema03.md con Kill Chain 2 corregida.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@_posts/2026-05-31-tema03.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corregir URL y añadir paso de verificación en Kill Chain 2</name>
  <files>_posts/2026-05-31-tema03.md</files>
  <action>
Modificar únicamente la sección "### Lab: Ejecución de código Groovy" (líneas 1255–1278).
Los cambios son:

A) ANTES del paso "1. Abrir el browser...", insertar un nuevo paso numerado "0" (o
   renumerar como paso 1 y correr los demás) titulado "Verificar que Jenkins está activo".
   Incluir:
   - Desde Metasploitable3 (PowerShell), dos comandos alternativos:
     `Get-Service | Where-Object {$_.DisplayName -like "*jenkins*"}` para confirmar estado del servicio,
     y `netstat -ano | findstr :8484` para confirmar que el puerto está escuchando.
   - Desde Kali (alternativa sin entrar a la VM objetivo):
     `curl -s -o /dev/null -w "%{http_code}" http://10.0.2.15:8484/`
     — debe responder 200 si Jenkins está levantado.
   Indicar que el alumno debe ver el servicio en estado "Running" / puerto 8484 en LISTEN
   antes de continuar.

B) Cambiar la URL en el paso de abrir el browser:
   - Texto actual: `http://10.0.2.15:8484/jenkins/script`
   - Texto correcto: `http://10.0.2.15:8484/script`
   Agregar una nota breve explicando por qué: Jenkins en Metasploitable3 corre standalone
   con Winstone/Jetty y el context path por defecto es `/`, no `/jenkins/`.

C) No modificar ninguna otra sección. Los pasos de Groovy (whoami, ipconfig) permanecen
   exactamente igual, incluyendo el comentario `<!-- output [ASSUMED] -->`.

La sección final debe quedar con esta estructura de pasos:
  1. Verificar que Jenkins está activo (nuevo — comandos PS + curl)
  2. Abrir browser en Kali → `http://10.0.2.15:8484/script`  (URL corregida)
  3. Escribir script Groovy `println "cmd /c whoami".execute().text`
  4. Clic en Run — output esperado
  5. Comandos Groovy más complejos (ipconfig)
  </action>
  <verify>
    <automated>grep -n "8484/script" /home/researcher/Teaching/untels/ollerenacastro.github.io/_posts/2026-05-31-tema03.md</automated>
  </verify>
  <done>
    - grep encuentra `8484/script` (sin `/jenkins/`) en la sección Kill Chain 2
    - grep NO encuentra `8484/jenkins/script` en el archivo
    - El archivo contiene `Get-Service` o `netstat -ano | findstr :8484` antes del paso de abrir el browser
    - El resto del post (Kill Chain 1, Kill Chain 3, tabla MITRE) está sin cambios
  </done>
</task>

</tasks>

<verification>
```bash
# 1. URL correcta presente
grep -n "8484/script" _posts/2026-05-31-tema03.md

# 2. URL incorrecta ausente
grep -c "jenkins/script" _posts/2026-05-31-tema03.md
# debe retornar 0

# 3. Paso de verificación presente
grep -n "Get-Service\|netstat.*8484\|findstr.*8484" _posts/2026-05-31-tema03.md

# 4. Jekyll parsea sin errores
bundle exec jekyll build --quiet 2>&1 | head -20
```
</verification>

<success_criteria>
- `grep -c "jenkins/script" _posts/2026-05-31-tema03.md` retorna 0
- `grep -c "8484/script" _posts/2026-05-31-tema03.md` retorna al menos 1
- Existe al menos un comando de verificación de servicio antes del paso de abrir el browser
- El post Jekyll sigue siendo válido (build sin errores de parsing)
</success_criteria>

<output>
Crear `.planning/quick/260531-f5e-corregir-kill-chain-2-en-tema03-url-corr/260531-f5e-SUMMARY.md` al terminar.
</output>
