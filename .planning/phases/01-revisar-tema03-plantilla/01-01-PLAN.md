---
phase: 01-revisar-tema03-plantilla
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
autonomous: true
requirements:
  - STR-03
tags:
  - jekyll
  - gitignore
  - housekeeping

must_haves:
  truths:
    - "El archivo .gitignore ignora cualquier archivo *.backup dentro de _posts/"
    - "git status no muestra _posts/2025-10-05-tema03.md.backup como untracked tras la edición"
  artifacts:
    - path: ".gitignore"
      provides: "Regla glob _posts/*.backup bajo sección 'Backups de posts'"
      contains: "_posts/*.backup"
  key_links:
    - from: ".gitignore"
      to: "_posts/2025-10-05-tema03.md.backup"
      via: "glob _posts/*.backup"
      pattern: "^_posts/\\*\\.backup$"
---

<objective>
Añadir una entrada al `.gitignore` para que cualquier archivo `*.backup` dentro de `_posts/` sea ignorado por Git. Esto implementa D-04 de CONTEXT.md y resuelve el archivo untracked `_posts/2025-10-05-tema03.md.backup` que aparece actualmente en `git status`.

Purpose: Mantener el repositorio limpio de backups locales de posts y prevenir commits accidentales de backups futuros. Sin esto, cada vez que se haga una revisión mayor de un post existirá un `.backup` untracked.

Output: `.gitignore` con una nueva sección `# Backups de posts` que contiene la línea `_posts/*.backup`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-revisar-tema03-plantilla/01-CONTEXT.md
@.planning/phases/01-revisar-tema03-plantilla/01-PATTERNS.md
@.gitignore

<interfaces>
Sección existente del .gitignore (líneas 25-36) — patrón de "sección con header `#` seguida de entradas":
# Misc
_sass/dist
assets/js/dist

# Claude Code
.claude/
CLAUDE.md
issues/
docs/plan_*.md
docs/methodology_*.md
backups/
todo.md

La nueva sección debe añadirse con el mismo estilo: línea en blanco, header con `#`, entradas glob.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir sección "Backups de posts" al .gitignore</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (estado actual: 36 líneas, última línea "todo.md")
    - .planning/phases/01-revisar-tema03-plantilla/01-PATTERNS.md (sección ".gitignore — añadir línea (MODIFICAR)", líneas 315-340)
    - .planning/phases/01-revisar-tema03-plantilla/01-CONTEXT.md (D-04, líneas 21-22)
  </read_first>
  <action>
    Implementa D-04 de CONTEXT.md. Usa Edit para añadir al final del archivo `.gitignore` (después de la línea 36 `todo.md`) un bloque de exactamente 3 líneas:
    1. Línea en blanco
    2. `# Backups de posts`
    3. `_posts/*.backup`

    Usa el patrón glob `_posts/*.backup` (no la ruta exacta `_posts/2025-10-05-tema03.md.backup`) para cubrir backups futuros, consistente con cómo otras secciones del archivo (`docs/plan_*.md`, `docs/methodology_*.md`) usan globs. No modifiques ninguna línea existente del archivo.

    Tras editar, ejecuta `git check-ignore -v _posts/2025-10-05-tema03.md.backup` para confirmar que la regla aplica al archivo concreto que motivó el cambio.
  </action>
  <verify>
    <automated>grep -Fx '_posts/*.backup' /home/researcher/Teaching/untels/ollerenacastro.github.io/.gitignore &amp;&amp; grep -Fx '# Backups de posts' /home/researcher/Teaching/untels/ollerenacastro.github.io/.gitignore &amp;&amp; git -C /home/researcher/Teaching/untels/ollerenacastro.github.io check-ignore _posts/2025-10-05-tema03.md.backup</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Fx '_posts/*.backup' .gitignore` exits 0 (literal exact line present)
    - `grep -Fx '# Backups de posts' .gitignore` exits 0 (header literal exact line present)
    - `git check-ignore _posts/2025-10-05-tema03.md.backup` exits 0 and prints the path (file is now ignored)
    - `git status --porcelain | grep -F '_posts/2025-10-05-tema03.md.backup'` exits 1 (no longer untracked)
    - `wc -l .gitignore` returns 39 (original 36 + blank + header + entry); if Git auto-trims trailing newline, accept 38 or 39
    - No previously existing line modified: `git diff .gitignore` shows only additions (lines prefixed with `+`), zero deletions (lines prefixed with `-`)
  </acceptance_criteria>
  <done>El .gitignore contiene la nueva sección, `_posts/2025-10-05-tema03.md.backup` ya no aparece en `git status`, y ningún otro contenido del .gitignore fue alterado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo local → GitHub remote | git push publica el .gitignore; un .gitignore mal escrito podría des-ignorar archivos sensibles |
| developer → repo | edición manual del archivo de configuración |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .gitignore | mitigate | Usar Edit (no Write) para preservar líneas existentes; `git diff` debe mostrar solo additions |
| T-01-02 | Tampering | .gitignore | mitigate | Acceptance criteria exige `git diff` sin deletions — bloquea overwrite accidental |
| T-01-03 | Information Disclosure | _posts/*.backup | accept | Los backups son locales y no contienen secretos; el patrón ignora archivos que de otro modo se subirían |
</threat_model>

<verification>
- `.gitignore` contiene `_posts/*.backup` como línea exacta
- `git check-ignore _posts/2025-10-05-tema03.md.backup` confirma la regla aplica
- `git diff .gitignore` muestra solo additions
- Ninguna otra línea preexistente del .gitignore fue modificada
</verification>

<success_criteria>
- D-04 implementado: backup queda fuera de Git
- `git status` no muestra `_posts/2025-10-05-tema03.md.backup` como untracked
- No se introdujeron cambios colaterales en el .gitignore
</success_criteria>

<output>
Create `.planning/phases/01-revisar-tema03-plantilla/01-01-SUMMARY.md` when done
</output>
