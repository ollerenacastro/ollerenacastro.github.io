---
context: default
phase: none — entre Phase 01 (cerrada) y Phase 02 (no iniciada)
task: null
total_tasks: null
status: ready_for_next_phase
last_updated: "2026-07-06T10:30:23Z"
---

<current_state>
Sesión ad-hoc de troubleshooting del lab **untels-tim-lab** (mini-OpenCTI) en la VM Kali.
NO fue phase work. Se diagnosticó a fondo por qué el TIM "siempre necesita reconstruirse",
se halló la causa raíz, y se creó + documentó el fix durable (`restart-lab.sh`).

**TIM operativo ahora:** HTTP 200, 1514 attack patterns intactos, login
`admin@tim.local` / `JetH1y1vSqySAQUxgdNR`. Phase 02 (tema04) sigue sin iniciar, igual
que antes.
</current_state>

<completed_work>

- **Diagnóstico 8080 muerto**: docker-proxy/NAT del host apuntaba a la IP vieja de opencti.
  Confirmado: `ss` → LISTEN 127.0.0.1:8080, `curl` → HTTP 000, `node` in-container → IN 401
  (app viva dentro, proxy no forwardea).
- **MITRE=0 = falsa alarma**: GraphQL directo da **1514 attackPatterns**. `verify-platform.sh`
  se rindió a los 15 min mientras opencti calentaba tras el force-recreate. Datos intactos.
- **Causa raíz recurrente**: al reanudar la VM, `restart:unless-stopped` NO respeta
  `depends_on` → opencti sube antes que ES/Redis, crashea, toma IP nueva → NAT stale (8080
  muerto) + worker/connectors en crash-loop (Errno 111 Connection refused). Hueco en logs
  23:12→07:09 = VM suspendida de noche.
- **`restart-lab.sh`** (commit `c60eb6d`): `down` + `up` limpio, reordena por healthcheck,
  espera 8080, self-check con curl loop.
- **README lab** (commit `5355311`, pusheado): nota de retomar + 3 filas de troubleshooting.
- **Post tema05** (commit `3079756`, **NO pusheado**): nota de retomar con restart-lab.sh.
</completed_work>

<remaining_work>

- **Push del blog** (commit `3079756`): pendiente de confirmación del usuario (publica a
  GitHub Pages, lo ven alumnos).
- **En la VM Kali**: `cd ~/untels-tim-lab && git pull` para bajar `restart-lab.sh` + README.
- **Phase 02: Reconocimiento (tema04)** — NO iniciada. Evaluar si tema04 OilRig satisface
  CONT-02. Ver ROADMAP.md §Phase 2.
</remaining_work>

<decisions_made>

- Fix del TIM = arranque limpio (`restart-lab.sh`: down+up), NO `force-recreate` ni `up -d`
  sueltos. `down` mata el NAT stale; `up` respeta depends_on. Datos persisten en volúmenes.
- NO tocar el binding `127.0.0.1:8080:8080` — navegar dentro de Kali (diseño: no exponer
  el TIM fuera de la VM).
- NO subir RAM ni `mem_limit` de Redis por ahora (ES 91% / Redis 90% es justo pero no roto;
  candidato si vuelve a flapear).
</decisions_made>

<blockers>
Ninguno. TIM funcional. Decisión de contenido de Phase 02 sigue pendiente (igual que antes).
</blockers>

## Required Reading (in order)
1. `.planning/ROADMAP.md §Phase 2` — goals, CONT-02, success criteria
2. `~/Research/untels-tim-lab/scripts/restart-lab.sh` + `README.md` — procedimiento de arranque
3. `_posts/2025-11-22-tema05.md` — clase TIM publicada (nota de retomar añadida)

## Critical Anti-Patterns (do NOT repeat these)
- [up -d / force-recreate SUELTOS tras suspend/reboot de la VM]: dejan la red Docker a medias
  → 8080 muerto (NAT apunta a IP vieja de opencti) aunque `ps` diga *healthy*. Mitigación:
  SIEMPRE `restart-lab.sh` (down+up limpio) al retomar el lab.
- [BACKUPS EN _posts/]: Jekyll publica cualquier archivo bajo `_posts/`. Backups SIEMPRE en
  `backups/` (excluido en `_config.yml`).

## Infrastructure State
- **TIM (VM Kali)**: OPERATIVO. HTTP 200, 1514 attackPatterns, feeds/CISA cargados.
  Login admin@tim.local / JetH1y1vSqySAQUxgdNR.
- **Repo lab untels-tim-lab**: `origin/main` en `5355311` (restart-lab.sh + README). La VM
  necesita `git pull`.
- **Blog**: `main` 1 commit adelante de `origin` (`3079756`, sin push).
- **VM Kali**: 8GB/4CPU, disco Docker 25GB. ES/Redis cerca de sus mem_limits — vigilar si flapea.

<context>
La sesión fue puro troubleshooting operativo del lab, no roadmap. El hallazgo clave: el
síntoma "reconstruir opencti" era en realidad NAT stale por suspend/resume de la VM, curado
por un arranque limpio (down+up), no por reconstrucción. Todo documentado en las dos guías.
El único pendiente "de curso" real sigue siendo Phase 02.
</context>

<next_action>
Al retomar, elegir rumbo:
1. **Cerrar loose ends**: confirmar push del blog (3079756) + recordar `git pull` en la VM.
2. **Curso/roadmap**: iniciar Phase 02 con `/gsd-plan-phase 02` (evaluar tema04 OilRig vs CONT-02).
3. **Clase TIM**: correr `./scripts/restart-lab.sh` en la VM y ensayar el guión de dictado.
</next_action>
